/.fluxbox/overlay

# Added by Claude Task Master
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific
.DS_Store
# Task files
tasks.json
tasks/
/.config/efck-chat-keyboard/recent_emojis.json
/.config/keyd/app.lock

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Safety copies left by bd-migrate-embedded-to-server; keep them on disk
# until the migrated workspace is trusted, but never commit them.
.beads-migration-backups/
.beads.embedded.*/
.beads.failed-server-restore.*/
.beads.failed-server-attempt/
