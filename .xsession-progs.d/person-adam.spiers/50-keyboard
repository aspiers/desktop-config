#!/bin/bash

host=${HOST:-${HOSTNAME:-`hostname`}}
host=${host%%.*}

setup-keyboard

here=`dirname $0`
. $here/00-systemd-user-env

# Ensure we only start after systemd --user environment is already set up
systemctl --user disable keyboard-watcher.service

systemctl --user start keyboard-watcher.service
