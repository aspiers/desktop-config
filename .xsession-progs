#!/bin/sh

host=${HOST:-$HOSTNAME}
host=${host%%.*}
exec 1> ~/.xsession-my-errors.${host} 2>&1

. ~/.shared_env

echo "### .xsession-progs ..."

. $ZDOTDIR/lib/libproc.sh

. $ZDOT_RUN_HOOKS .xsession-progs.d &

