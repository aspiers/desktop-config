#!/bin/bash

inxi=~/tmp/.inxi.Gxx
inxi -Gxx -c 0 > $inxi

if grep -q 'res: 3840x2160' $inxi &&
        [ `monitors-connected` -ge 2 ]
then
    exit 0
fi

if grep -q 'BenQ BL3200 res: 2560x1440' $inxi; then
    exit 0
fi

exit 1
