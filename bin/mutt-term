#!/bin/sh

# Keep the title fixed so Fluxbox can identify and place the window.
kitty \
    --override initial_window_width=150c \
    --override initial_window_height=50c \
    --title "mutt: $1" \
    -- \
    "$@"
