#!/bin/sh

host=${HOST:-${HOSTNAME:-`hostname`}}
host=${host%%.*}
display_file=$HOME/.Xdisplay.$host
if [ -e $display_file ]; then
  . "$display_file"
  "$@"
else
  echo "$display_file does not exist; aborting." >&2
  exit 1
fi
