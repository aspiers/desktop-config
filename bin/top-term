#!/bin/sh

. $ZDOTDIR/lib/libfonts.sh

#Eterm -t top
unset COLUMNS
cmd="${1:-top}"
[ -n "$1" ] && shift

geometry=90x40
case "$cmd" in
    htop) geometry=150x50 ;;
esac

urxvt-256color -fn $small_font -geometry $geometry -e "$cmd" "$@"
