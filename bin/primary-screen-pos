#!/bin/bash

eval $(get-screen-info)

case "$primary_screen_pos" in
    left)
        echo 0
        ;;
    right)
        echo 1
        ;;
    *)
        echo >&2 "ERROR: Unrecognised value '$primary_screen_pos' for primary_screen_pos"
        exit 1
esac
