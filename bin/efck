#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.8"
# dependencies = [
#   "efck[extra]",
#   "pyqt6",
# ]
# ///

import subprocess
import sys

subprocess.run([sys.executable, "-m", "efck"])