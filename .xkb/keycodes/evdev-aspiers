default
xkb_keycodes "aspiers" {
    include "evdev(evdev)"

    // There will be warnings from xkbcomp because of multiple definitions.

    <LCTL> = 64;
    <AB01> = 37;
    <LALT> = 133;
};

xkb_keycodes "kinesis-360" {
    include "evdev(evdev)"

    // There will be warnings from xkbcomp because of multiple definitions.

    <RWIN> = 133;
};
