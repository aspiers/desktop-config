(if (contains (geometry) "1440x900+")
    (undecorate))
(if (and (is (window_role) "conversation")
         (or (is (window_class) "Gaim")
             (is (window_class) "Pidgin")))
   (begin (pin)))
