[config]
# Redshift owns live CRTC colour-temperature/gamma ramps. Treating those
# transient values as display-profile state makes an otherwise loaded profile
# appear different and can make autorandr overwrite Redshift's current ramp.
skip-options=gamma
