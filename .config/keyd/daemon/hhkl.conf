# HHKL (Happy Hacking Keyboard Lite) configuration
[ids]
0853:0100

[main]
include partials/left-alpha-common

# via hhkl-left-alpha
include partials/xcv3

# hhkl-qaz (different from standard-qaz!)
esc = q
leftcontrol = a
# z already handled above

# hhkl-escape
grave = esc

# hhkl-mods
leftalt = layer(alt)  # stays as alt
leftmeta = layer(control)
backslash = layer(alt)
