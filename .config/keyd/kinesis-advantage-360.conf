# Kinesis Advantage 360 configuration
[ids]
05f3:0081

[main]

include kinesis-left-thumb-pad

# Right thumb pad (different from original Kinesis!)
rightmeta = compose
rightalt = layer(alt)
rightcontrol = rightmeta

esc = minus
capslock = insert

# Note: up/down swap handled in firmware for 360

# Top-right key (AE11)
equal = S-3
