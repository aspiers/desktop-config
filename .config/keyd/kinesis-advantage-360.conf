# Kinesis Advantage 360 configuration
[ids]
1d50:615e
29ea:0362

[main]

include kinesis-left-thumb-pad

# Right thumb pad (different from original Kinesis! so doesn't
# use kinesis-right-thumb-pad)
leftmeta = compose
rightcontrol = layer(meta)

esc = minus
capslock = insert

# Note: up/down swap handled in firmware for 360

include minus-sterling-euro
include gb
